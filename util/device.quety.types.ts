export  enum deviceConnectionStatus {
    disconnected = "disconnected",
    connected = "connected",
}

export enum deviceSortOptions {
    latest = "latest",
    oldest = "oldest",
    asc = "a-z",
    desc = "z-a"
}